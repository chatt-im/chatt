{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:embedded": "bun run build && bun gen-embed.mjs",
    "preview": "vite preview",
    "emoji:generate": "node scripts/generate-emoji.mjs",
    "emoji:size": "node scripts/check-emoji-size.mjs"
  },
  "dependencies": {
    "solid-js": "^1.9.3"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^6.0.5",
    "vite-plugin-solid": "^2.11.0"
  }
}
