[package]
name = "benchmark"
version = "0.1.0"
edition = "2024"

[dependencies]
jsony_bench = { path = "../../../jsony/crates/jsony_bench" }
nnnoiseless = { version = "0.5.2", default-features = false }
opus-codec = { path = "../opus-codec", features = ["dred"] }
sonora = "0.1.0"
chatt = { path = "../.." }
