[package]
name = "rpc"
version = "0.1.0"
edition = "2024"

[dependencies]
jsony = "0.1.10"
libc = "0.2"
ring = "0.17.14"
toml-spanner = { version = "1.0.3", features = ["to-toml", "from-toml", "derive"] }
